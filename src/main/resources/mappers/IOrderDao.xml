<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yates.dao.IOrderDao">
    <insert id="insertOrder" parameterType="order">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into t_seckill_order(productid, payamount, userid, transquantity, merchantid, createtime, paystatus, recevingaddress, recevingphone, recevingname, transserial)
        value
        (#{productId}, #{payAmount}, #{userId}, #{transQuantity}, #{merchantId}, #{createTime}, #{payStatus}, #{recevingAddress}, #{recevingPhone}, #{recevingName}, #{transSerial})
    </insert>
</mapper>